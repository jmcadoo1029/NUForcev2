import { InProgressCard } from './InProgressCard'

// In Progress view — the shared, team-wide board of what's actively being worked
// (open_quotes). Its own tab so it stays visible to everyone rather than buried.
export function InProgress() {
  return <InProgressCard />
}
